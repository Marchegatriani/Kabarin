<?xml version="1.0" encoding="utf-8"?>
<!--
    item_news.xml  →  digunakan sebagai item_latest_news.xml
    Tampilan: card putih, gambar penuh di atas, badge + judul + deskripsi di bawah
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="16dp"
    app:cardCornerRadius="14dp"
    app:cardElevation="3dp"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#FFFFFF">

        <!-- ── Gambar Berita ── -->
        <ImageView
            android:id="@+id/ivNewsImage"
            android:layout_width="match_parent"
            android:layout_height="185dp"
            android:scaleType="centerCrop"
            android:contentDescription="News image"
            android:src="@android:drawable/ic_menu_gallery" />

        <!-- ── Konten Teks ── -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:paddingTop="12dp"
            android:paddingBottom="14dp">

            <!-- Baris: Badge Kategori + Estimasi Waktu Baca + Tombol Simpan -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginBottom="8dp">

                <TextView
                    android:id="@+id/tvNewsCategory"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="WORLD"
                    android:textSize="10sp"
                    android:textStyle="bold"
                    android:textColor="#FFFFFF"
                    android:background="@drawable/badge_category"
                    android:paddingStart="8dp"
                    android:paddingEnd="8dp"
                    android:paddingTop="3dp"
                    android:paddingBottom="3dp" />

                <TextView
                    android:id="@+id/tvReadTime"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="6 min read"
                    android:textSize="12sp"
                    android:textColor="#7A8A9B"
                    android:layout_marginStart="10dp" />

                <View
                    android:layout_width="0dp"
                    android:layout_height="0dp"
                    android:layout_weight="1" />

                <ImageView
                    android:id="@+id/ivSave"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:src="@drawable/ic_save"
                    android:padding="4dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Save news"
                    app:tint="#A0A8B0" />

            </LinearLayout>

            <!-- Judul Berita -->
            <TextView
                android:id="@+id/tvNewsTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="News Title Goes Here"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="#0D1B2A"
                android:maxLines="3"
                android:ellipsize="end"
                android:lineSpacingMultiplier="1.25"
                android:layout_marginBottom="6dp" />

            <!-- Deskripsi Singkat -->
            <TextView
                android:id="@+id/tvNewsDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Short description of the article goes here..."
                android:textSize="13sp"
                android:textColor="#5A6878"
                android:maxLines="2"
                android:ellipsize="end"
                android:lineSpacingMultiplier="1.4" />

        </LinearLayout>
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
